language: generic

os:
  - linux
  - windows

services:
  - docker

env:
  global:
    - DOCKER_IMAGE=gographics/imagick.v3:im-$IMAGEMAGICK7_VERSION
    - CACHE_DIR=$HOME/.cache
    - GOCACHE=$CACHE_DIR/go-build
    - GOPATH=$CACHE_DIR/go

before_cache:
  - |-
    case $TRAVIS_OS_NAME in
      windows)
        # https://unix.stackexchange.com/a/137322/107554
        $msys2 pacman --sync --clean --noconfirm
        ;;
    esac

cache:
  directories:
    - $GOCACHE
    - $GOPATH/pkg
    - $HOME/AppData/Local/Temp/chocolatey
    - /C/tools/msys64

before_script:
  - |-
    echo "Setting up $TRAVIS_OS_NAME"
    case $TRAVIS_OS_NAME in
      linux)
        docker pull "$DOCKER_IMAGE"
        ;;
      windows)
        [[ ! -f C:/tools/msys64/msys2_shell.cmd ]] && rm -rf C:/tools/msys64
        choco uninstall -y mingw
        choco upgrade --no-progress -y msys2
        export msys2='cmd //C RefreshEnv.cmd '
        export msys2+='& set MSYS=winsymlinks:nativestrict '
        export msys2+='& C:\\tools\\msys64\\msys2_shell.cmd -defterm -no-start'
        export mingw64="$msys2 -mingw64 -full-path -here -c "\"\$@"\" --"
        export msys2+=" -msys2 -c "\"\$@"\" --"
        $msys2 pacman --sync --noconfirm --needed mingw-w64-x86_64-toolchain
        ## Install more MSYS2 packages from https://packages.msys2.org/base here
        $msys2 pacman -S mingw-w64-x86_64-pkg-config
        $msys2 pacman -S mingw-w64-x86_64-imagemagick
        $msys2 pacman -S mingw-w64-x86_64-gcc
        taskkill //IM gpg-agent.exe //F  # https://travis-ci.community/t/4967
        export PATH=/C/tools/msys64/mingw64/bin:$PATH
        export PKG_CONFIG_PATH=/C/tools/msys64/mingw64/lib/pkgconfig
        export MAKE=mingw32-make  # so that Autotools can find it
        ;;
    esac

script:
  - |-
    echo "Executing under $TRAVIS_OS_NAME"
    case $TRAVIS_OS_NAME in
      linux)
        docker run -it --rm \
          -v $PWD:/go/projects/imagick \
          -v ${GOCACHE}:/root/.cache/go-build \
          -v ${GOPATH}/pkg:/go/pkg \
          "$DOCKER_IMAGE"
        ;;
      windows)
        # TODO
        ;;
    esac
